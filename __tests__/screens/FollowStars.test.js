import { render } from '@testing-library/react-native';
import FollowStars from '../../screens/FollowStars';

test('renders FollowStars component without crashing', () => {
  render(<FollowStars />);
});
